var greeting = "Hello, everybody!"; // defines 'greeting'

var specialGuest = "Neil deGrasse Tyson"

var greetSpecialGuest = "Hello, " + specialGuest + "!"; // concatenates strings to greet a special guest in 'greetSpecialGuest'

var topic = "space";

var conversation = `Let's talk about ${topic}.`;  // interpolates a string in 'conversation' 
